import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'الخصومات' };

export default function DeductionsPage() {
  return (
    <div className="text-sm text-muted">
      وحدة «الخصومات» قيد الإنشاء.
    </div>
  );
}
