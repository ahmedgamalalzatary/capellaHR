ALTER TABLE `attendance_sessions` DROP CONSTRAINT `attendance_sessions_checkout_state`;--> statement-breakpoint
ALTER TABLE `attendance_sessions` ADD CONSTRAINT `attendance_sessions_automatic_timeout_state` CHECK ((`attendance_sessions`.`automatic_timeout_at` is null and `attendance_sessions`.`automatic_timeout_corrected_at` is null and `attendance_sessions`.`flagged` = false) or (`attendance_sessions`.`automatic_timeout_at` = timestampadd(hour, 16, `attendance_sessions`.`check_in_at`) and `attendance_sessions`.`flagged` = true and `attendance_sessions`.`check_out_at` is not null and ((`attendance_sessions`.`automatic_timeout_corrected_at` is null and `attendance_sessions`.`check_out_at` = `attendance_sessions`.`automatic_timeout_at`) or (`attendance_sessions`.`automatic_timeout_corrected_at` >= `attendance_sessions`.`automatic_timeout_at`))));--> statement-breakpoint
ALTER TABLE `attendance_sessions` ADD CONSTRAINT `attendance_sessions_checkout_state` CHECK ((`attendance_sessions`.`check_out_at` is null and `attendance_sessions`.`worked_minutes` is null and `attendance_sessions`.`overtime_minutes` is null and `attendance_sessions`.`shortage_minutes` is null) or (`attendance_sessions`.`check_out_at` is not null and `attendance_sessions`.`check_out_at` > `attendance_sessions`.`check_in_at` and `attendance_sessions`.`worked_minutes` = timestampdiff(minute, `attendance_sessions`.`check_in_at`, `attendance_sessions`.`check_out_at`) and `attendance_sessions`.`overtime_minutes` = greatest(`attendance_sessions`.`worked_minutes` - `attendance_sessions`.`required_minutes`, 0) and `attendance_sessions`.`shortage_minutes` = greatest(`attendance_sessions`.`required_minutes` - `attendance_sessions`.`worked_minutes`, 0)));