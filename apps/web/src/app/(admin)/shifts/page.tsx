import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'الورديات' };

export default function ShiftsPage() {
  return (
    <div className="text-sm text-muted">
      وحدة «الورديات» قيد الإنشاء.
    </div>
  );
}
