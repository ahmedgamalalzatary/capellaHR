# Web feature convention

Each HR feature may own its API calls, components, hooks, schemas, types, and utilities. Features may import shared workspace packages and common app code, but should not import another feature's internal files.
