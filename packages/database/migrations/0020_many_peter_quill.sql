ALTER TABLE `attendance_denied_attempts` DROP CONSTRAINT `attendance_denied_attempts_approval_state`;--> statement-breakpoint
ALTER TABLE `attendance_denied_attempts` ADD `dismissed_at` timestamp(3);--> statement-breakpoint
ALTER TABLE `attendance_denied_attempts` ADD CONSTRAINT `attendance_denied_attempts_approval_state` CHECK ((`attendance_denied_attempts`.`approved_at` is null and `attendance_denied_attempts`.`approved_session_id` is null and `attendance_denied_attempts`.`dismissed_at` is null) or (`attendance_denied_attempts`.`approved_at` is not null and `attendance_denied_attempts`.`approved_session_id` is not null and `attendance_denied_attempts`.`employee_id` is not null and `attendance_denied_attempts`.`dismissed_at` is null) or (`attendance_denied_attempts`.`approved_at` is null and `attendance_denied_attempts`.`approved_session_id` is null and `attendance_denied_attempts`.`dismissed_at` is not null));