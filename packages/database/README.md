# Database package

Drizzle schemas are grouped by HR domain under `src/schema`. The root schema registry exports tables for Drizzle Kit. Migrations are generated into `migrations`, and seed routines live under `src/seed`.
