import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'التقارير' };

export default function ReportsPage() {
  return (
    <div className="text-sm text-muted">
      وحدة «التقارير» قيد الإنشاء.
    </div>
  );
}
