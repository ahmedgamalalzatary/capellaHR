import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'السلف' };

export default function AdvancesPage() {
  return (
    <div className="text-sm text-muted">
      وحدة «السلف» قيد الإنشاء.
    </div>
  );
}
